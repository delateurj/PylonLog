<Window x:Class="PylonLog.Core.wndMaintainRelatedData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PylonLog.Core"
        mc:Ignorable="d"
        Title="wndMaintainRelatedData" Height="300" Width="300">

    <StackPanel>
        <Grid>
        <DataGrid x:Name="dgMaintainRelatedData" DataContext="{StaticResource plugViewSource}" ItemsSource="{Binding}">
         
        </DataGrid>
        <!--<ComboBox x:Name="dgCmboPlugType" MinWidth="85"
                                            ItemsSource = "{Binding Source={StaticResource plugViewSource}}"
                                            SelectedValue="{Binding plugType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,NotifyOnValidationError =true, ValidatesOnExceptions=true}"
                                            DisplayMemberPath="name" 
                                            IsSynchronizedWithCurrentItem="false"/>-->
    </Grid>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnSavePlugs" Content="Save" Margin="0,10,0,0" MaxWidth="350" Click="btnSavePlugs_Click" />
           </StackPanel>
    </StackPanel>
</Window>
